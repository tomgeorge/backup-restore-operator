apiVersion: backups.example.com/v1alpha1
kind: VolumeBackupProvider
metadata:
  name: cluster-level-volume-backup-provider
spec:
  providerType: s3
  #  image: busybox
  #env:
  #  - name: AWS_ACCESS_KEY_ID
  #    valueFrom:
  #      secretKeyRef:
  #        name: s3-credentials-secret
  #        value: aws_access_key_id
  #  - name: AWS_SECRET_ACCESS_KEY
  #    valueFrom:
  #      secretKeyRef:
  #        name: s3-credentials-secret
  #        value: aws_secret_access_key



